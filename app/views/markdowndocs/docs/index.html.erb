<%# Host override of markdowndocs gem's docs/index — see docs/show.html.erb
    for the rationale (semantic tokens flow through host's variable system). %>
<div class="min-h-screen bg-surface">
  <div class="max-w-7xl mx-auto px-4 py-12
        sm:px-6
        lg:px-8"
    <% if @search_enabled %>
       data-controller="docs-search"
       data-docs-search-index-url-value="<%= markdowndocs.search_index_path %>"
    <% end %>>
    <!-- Hero Section -->
    <div class="text-center mb-12">
      <h1 class="text-4xl font-bold text-text-heading mb-4">Documentation</h1>
      <p class="text-xl text-text-body">Browse our comprehensive documentation and guides</p>

      <% if @search_enabled %>
        <!-- Search input -->
        <div class="mt-6 max-w-lg mx-auto">
          <div class="relative">
            <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-text-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
            <input
              type="text"
              data-docs-search-target="input"
              data-action="input->docs-search#search"
              placeholder="<%= t("markdowndocs.search_placeholder", default: "Search documentation...") %>"
              class="w-full pl-10 pr-4 py-3 rounded-lg border border-border-strong shadow-sm bg-surface-raised text-text-heading transition-colors
                  placeholder:text-text-body
                  focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
              autocomplete="off" />
          </div>
        </div>
      <% end %>
    </div>

    <% if @docs_by_category.values.flatten.any? %>
      <!-- No results message -->
      <% if @search_enabled %>
        <div data-docs-search-target="noResults" class="hidden text-center py-12">
          <svg class="mx-auto h-12 w-12 text-text-muted" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
          <h3 class="mt-2 text-sm font-medium text-text-heading"><%= t("markdowndocs.no_search_results", default: "No matching documents") %></h3>
          <p class="mt-1 text-sm text-text-body"><%= t("markdowndocs.try_different_search", default: "Try a different search term") %></p>
        </div>
      <% end %>

      <!-- Documentation Cards by Category -->
      <% @docs_by_category.each do |category, docs| %>
        <% if docs.any? %>
          <div id="<%= category.parameterize %>" class="mb-12 scroll-mt-6"
            <% if @search_enabled %>data-docs-search-target="category"<% end %>>
            <h2 class="text-2xl font-semibold text-text-heading mb-6"><%= category %></h2>
            <div class="grid grid-cols-1 gap-6
                md:grid-cols-2
                lg:grid-cols-3">
              <% docs.each do |doc| %>
                <div <% if @search_enabled %>data-docs-search-target="card" data-slug="<%= doc.slug %>"<% end %>>
                  <%= render "markdowndocs/docs/card", doc: doc %>
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      <% end %>
    <% else %>
      <!-- Empty State -->
      <div class="bg-surface-raised rounded-lg shadow-sm">
        <div class="px-4 py-5
              sm:p-6">
          <div class="text-center py-12">
            <svg class="mx-auto h-12 w-12 text-text-body" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
            </svg>
            <h3 class="mt-2 text-sm font-medium text-text-heading">No documentation available</h3>
            <p class="mt-1 text-sm text-text-body">Documentation will appear here once markdown files are added.</p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
