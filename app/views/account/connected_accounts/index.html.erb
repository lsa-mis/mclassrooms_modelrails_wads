<% content_for(:title) { t("account.connected_accounts.index.title") } %>
<div class="max-w-2xl mx-auto px-4 py-16">
  <h1 class="text-2xl font-bold text-slate-900 dark:text-gray-100">
    <%= t("account.connected_accounts.index.title") %>
  </h1>

  <div class="mt-8 space-y-4">
    <% @authentications.each do |auth| %>
      <div class="flex items-center justify-between p-4 border border-gray-200 dark:border-gray-700 rounded-lg">
        <div>
          <p class="font-medium text-slate-900 dark:text-gray-100">
            <%= auth.provider.titleize %>
          </p>
          <p class="text-sm text-slate-600 dark:text-gray-400">
            <%= auth.email? ? t("account.connected_accounts.index.email_provider") : auth.uid %>
          </p>
          <% if auth.verified? %>
            <span class="text-xs text-green-700 dark:text-green-400"><%= t("account.connected_accounts.index.verified") %></span>
          <% end %>
        </div>
        <% if @authentications.count > 1 %>
          <%= button_to t("account.connected_accounts.index.disconnect"),
                account_connected_account_path(auth),
                method: :delete,
                class: "text-sm text-red-600 dark:text-red-400 underline hover:no-underline
                        focus:outline-none focus:ring-2 focus:ring-red-500 rounded" %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
