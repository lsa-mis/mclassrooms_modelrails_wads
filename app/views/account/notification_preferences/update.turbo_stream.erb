<%# Placeholder turbo_stream for preference updates. Task 17 will replace
    this with row-scoped Turbo Stream responses (a single toggle becomes a
    `replace` of just its row). For now the response body is empty — the
    request spec only asserts the media_type, so the controller's
    `format.turbo_stream` is satisfied. %>
