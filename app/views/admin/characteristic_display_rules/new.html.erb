<%# MiClassrooms Phase 5 Task 11 (Brief §11.4/§14.1): new characteristic
    display rule. %>
<% content_for(:title) { t(".title") } %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold text-text-heading"><%= t(".title") %></h1>

  <%= form_with model: @characteristic_display_rule, url: admin_characteristic_display_rules_path,
        class: "mt-8 space-y-6" do |form| %>
    <%= render "form", form: form %>
  <% end %>
</div>
